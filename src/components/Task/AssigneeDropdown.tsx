import React from 'react';
import {StyleSheet, Text, View} from 'react-native';

const AssigneeDropdown = () => {
  return (
    <View>
      <Text>AssigneeDropdown</Text>
    </View>
  );
};

export default AssigneeDropdown;

const styles = StyleSheet.create({});
